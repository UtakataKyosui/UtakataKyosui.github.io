
- 00:35 `scaffold`使用時、`--api`オプションをつけると、Viewが作成されず、JSONだけで返されるAPIモードでコントローラやルーティングが作られる
- 00:47 
	```sh
	cargo loco generate scaffold comment content:text <table名>:references --api
	```
	というように実行すると、テーブル間での関係性が形成できる 
- 06:22
	デフォルトではUserがあるので、多分こうなるはず
	```sh
	cargo loco generate scaffold medicine name:text count:int user:references --api
	```
- 06:27 
	```sh
	Error: DB(Custom("Migration file of version 'm20250427_060706_medicines' is missing, this migration has been applied but its file is missing"))
	Error: Generators(Message("failed to run loco db migration. error details: `command [\"cargo\", \"loco-tool\", \"db\", \"migrate\"] exited with code 1`"))
	```
	Migrationをリセットしろということ？ 
- 06:29 
	```
	cargo loco db reset
	```
	してみる 
- 06:30 
	この状態でroutesを確認してみる
	```sh
	cargo loco routes
	```
	 
- 06:32 
	```
	/_health
	  └─ GET        /_health
	/_ping
	  └─ GET        /_ping
	/api
	  ├─ GET        /api/auth/current
	  ├─ POST       /api/auth/forgot
	  ├─ POST       /api/auth/login
	  ├─ POST       /api/auth/magic-link
	  ├─ GET        /api/auth/magic-link/{token}
	  ├─ POST       /api/auth/register
	  ├─ POST       /api/auth/reset
	  ├─ GET        /api/auth/verify/{token}
	  ├─ GET        /api/medicines
	  │  POST       /api/medicines
	  │
	  ├─ GET        /api/medicines/{id}
	  │  PUT        /api/medicines/{id}
	  │  PATCH      /api/medicines/{id}
	  └─ DELETE     /api/medicines/{id}
	  ```
	  ちゃんと動いている。うれしい
	 
- 06:46
	```sh
	cargo loco start
	```
	して、
	いったん
	```sh
	curl --location '127.0.0.1:5150/api/auth/register' \
	     --header 'Content-Type: application/json' \
	     --data-raw '{
	         "name": "Loco user",
	         "email": "user@loco.rs",
	         "password": "12341234"
	     }'
	```
	してみる
- 06:55 
	```sh
	curl --location '127.0.0.1:5150/api/auth/login' \
	     --header 'Content-Type: application/json' \
	     --data-raw '{
	         "email": "user@loco.rs",
	         "password": "12341234"
	     }'
	```
	これでログインしてみる 
- 06:57
	```json
	{
	"token":"eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzUxMiJ9.eyJwaWQiOiI2YmQxOWJkYi0xYzM3LTQ1MmEtYTNiZS1kZmEwYTQ1OWRmMTQiLCJleHAiOjE3NDYzOTU3NTF9.NCgoMkYHJ-XVbuqMROILYEidcnb719VZInXcx-4JFgat_zNEXpG-0GHr6q4vY2_1gdaZTY8VnbZHso7auCvGtw",
		"pid":"6bd19bdb-1c37-452a-a3be-dfa0a459df14",
		"name":"Loco user",
		"is_verified":false
	}
	```
- 07:01
	```sh
	curl --location '127.0.0.1:5150/api/auth/current' 
	```
	すると
	```sh
	{"error":"unauthorized","description":"You do not have permission to access this resource"}
	```
- 07:06
	```sh
	curl --location --request GET '127.0.0.1:5150/api/auth/current' \
	     --header 'Content-Type: application/json' \
	     --header 'Authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzUxMiJ9.eyJwaWQiOiI2YmQxOWJkYi0xYzM3LTQ1MmEtYTNiZS1kZmEwYTQ1OWRmMTQiLCJleHAiOjE3NDYzOTU3NTF9.NCgoMkYHJ-XVbuqMROILYEidcnb719VZInXcx-4JFgat_zNEXpG-0GHr6q4vY2_1gdaZTY8VnbZHso7auCvGtw'
	```
	を投げてみると
	```sh
	{"pid":"6bd19bdb-1c37-452a-a3be-dfa0a459df14","name":"Loco user","email":"user@loco.rs"}
	```
- 07:08 う〜ん、めっちゃわかりづらいなこれ
- 07:09 
	先にutoipaを実装すべきだ。
	そうじゃないとわかりづらい 
- 07:10 
	```sh
	cargo add utoipa --features axum_extras ;
	cargo add utoipa-gen --features axum_extras ;
	cargo add utoipa-swagger-ui --features axum ;
	cargo add utoipa_redoc --features axum ;
	cargo add utoipa_rapidoc --features axum
	```
	 
- 07:13 これ記事の方は結構雑に実装しているな
- 07:14 `src/models/*.rs`,`src/views/*.rs`にある構造体に`ToSchema`を導入すればよい
- 07:16
	```rust
	#[derive(Debug, Deserialize, Serialize)]
	pub struct LoginParams {
		pub email: String,
		pub password: String,
	}
	
	#[derive(Debug, Deserialize, Serialize)]
	pub struct RegisterParams {
		pub email: String,
		pub password: String,
		pub name: String,
	}
	
	#[derive(Debug, Validate, Deserialize)]
	pub struct Validator {
		#[validate(length(min = 2, message = "Name must be at least 2 characters long."))]
		pub name: String,
		#[validate(custom(function = "validation::is_valid_email"))]
		pub email: String,
	}
	```
	これが
	```rust
	#[derive(Debug, Deserialize, Serialize,ToSchema)]
	pub struct LoginParams {
		pub email: String,
		pub password: String,
	}
	
	#[derive(Debug, Deserialize, Serialize,ToSchema)]
	pub struct RegisterParams {
		pub email: String,
		pub password: String,
		pub name: String,
	}
	
	#[derive(Debug, Validate, Deserialize,ToSchema)]
	pub struct Validator {
		#[validate(length(min = 2, message = "Name must be at least 2 characters long."))]
		pub name: String,
		#[validate(custom(function = "validation::is_valid_email"))]
		pub email: String,
	}
	```
	こうなる
- 07:29 
	`src/initializers/openapi_utoipa.rs`を作成し、
	```rust
	use axum::{async_trait, Router as AxumRouter};
	use loco_rs::{
	    app::{AppContext, Initializer}, Result,
	};
	
	use utoipa::OpenApi;
	use utoipa_rapidoc::RapiDoc;
	use utoipa_redoc::{Redoc, Servable};
	use utoipa_swagger_ui::SwaggerUi;
	
	use crate::controllers;
	use crate::controllers::article::{Params, RequestBodyById, ResponseBodyArticles};
	
	pub struct OpenApiUtoipaInitializer;
	#[async_trait]
	impl Initializer for OpenApiUtoipaInitializer {
	    fn name(&self) -> String {
	        "openapi-utoipa".to_string()
	    }
	
	    async fn after_routes(&self, mut router: AxumRouter, _ctx: &AppContext) -> Result<AxumRouter> {
	        router = router
	            .merge(SwaggerUi::new("/swagger-ui").url("/api-docs/openapi.json", ApiDoc::openapi()))
	            .merge(Redoc::with_url("/redoc", ApiDoc::openapi()))
	            .merge(RapiDoc::new("/api-docs/openapi.json").path("/rapidoc"));
	        Ok(router)
	    }
	}
	
	#[derive(OpenApi)]
	#[openapi(
	    paths(
	        controllers::article::index,
	        controllers::article::list,
	        controllers::article::get_one,
	        controllers::article::add,
	        controllers::article::update,
	        controllers::article::remove,
	    ),
	    components(schemas(Params, RequestBodyById, ResponseBodyArticles,))
	)]
	pub struct ApiDoc;
	``` 
- 07:30 
	https://zenn.dev/usagram/articles/d0172d9b770bff#openapi%EF%BC%88utoipa%EF%BC%89
	
	ここに書いてあるのを調整しながら、自分で調整して実装する 
- 22:14 chrono?の型はToSchema実装を追加してやらねばならぬ。 
- 22:15 いったん、Misskey自鯖のことを考えたい 